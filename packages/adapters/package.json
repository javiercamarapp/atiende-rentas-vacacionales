{
  "name": "@atiende-rv/adapters",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@atiende-rv/domain": "0.0.0"
  },
  "devDependencies": {
    "@atiende-rv/db": "0.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.0.5"
  }
}
