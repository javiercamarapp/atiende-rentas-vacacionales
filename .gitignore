# Dependencias
node_modules/
.pnp
.pnp.js

# Build/artefactos
dist/
build/
*.tsbuildinfo
coverage/

# Entorno y secretos — nunca commitear
.env
.env.*
!.env.example

# Logs (excepto el log operativo de Fase 1, que sí se versiona)
*.log
!docs/logs/bucle.log

# Sistema operativo / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Bases de datos embebidas de prueba (embedded-postgres/PGlite)
.pgdata/
*.pgdata/

# Playwright (Lote 4): artefactos de ejecución, nunca versionados
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/

# Lote 3.3 (despliegue): `/api/index.js` es un artefacto GENERADO por
# `npm run build:api-vercel` (deploy/build-api-vercel.mjs) a partir de
# `apps/api/api/index.ts` — nunca se edita ni se versiona a mano, igual
# que `apps/web/dist/`.
/api/
.vercel
.env*
